import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "pharmacopeia · an open API for medications",
    template: "%s · pharmacopeia",
  },
  description:
    "Drugs, classes, interactions, and indications — structured, versioned, free. A developer-first reference layer for the world's pharmacopeia. Inspired by PokeAPI.",
  metadataBase: new URL("https://pharmacopeia.dev"),
  openGraph: {
    title: "pharmacopeia · an open API for medications",
    description:
      "Drugs, classes, interactions, and indications — structured, versioned, free.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="flex min-h-full flex-col bg-background font-sans">
        <SiteHeader />
        <main className="flex-1">{children}</main>
        <SiteFooter />
      </body>
    </html>
  );
}
